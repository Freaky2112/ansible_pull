- hosts: localhost
  connection: local
  become: true

  pre_tasks:
    - name: update repositories
      apt:
        update_cache: yes
        changed_When: false

#  tasks:
#    - include: 
#       tasks/packages.yml
       # tasks/bash.yml

